pre-commit:
  commands:
    typecheck:
      run: bun run typecheck
    fmt:
      run: bunx oxfmt .
      stage_fixed: true
    lint:
      run: bun run lint
    build:
      run: bun run build
    gitleaks:
      run: gitleaks protect --staged
